apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-feed
spec:
  selector:
    matchLabels:
      run: api-feed
  replicas: 2
  template:
    metadata:
      labels:
        run: api-feed
    spec:
      containers:
      - name: api-feed
        image: elcharitas/udagram-api-feed:v1
        env:
          - name: POSTGRES_HOST
            valueFrom:
              configMapKeyRef:
                name: env-secrets
                key: POSTGRES_HOST
          - name: POSTGRES_USERNAME
            valueFrom:
              configMapKeyRef:
                name: env-secrets
                key: POSTGRES_USERNAME
          - name: POSTGRES_PASSWORD
            valueFrom:
              configMapKeyRef:
                name: env-secrets
                key: POSTGRES_PASSWORD
          - name: POSTGRES_DB
            valueFrom:
              configMapKeyRef:
                name: env-secrets
                key: POSTGRES_DB
          - name: JWT_SECRET
            valueFrom:
              configMapKeyRef:
                name: env-secrets
                key: JWT_SECRET
        resources:
          requests:
            memory: "100Mi"
          limits:
            memory: "500Mi"
        ports: 
        - containerPort: 8080
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m
